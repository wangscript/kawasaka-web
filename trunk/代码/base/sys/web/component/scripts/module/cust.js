Wade.cust=(function(){ var dom = Wade.dom; var dialog = Wade.dialog; var validate = Wade.validate; return {    selectGroupMembers : function() {   var userids = "", custids = "", custnames = "", custcodes = "", custmanagers = "";      var custs = dom.getElements("custs");   for (var i=0; i<custs.length; i++) {    var cust = custs[i];    if (cust.checked) {     var custprop = cust.value.split(";");         userids += (userids == "" ? "" : ",") + custprop[0];     custids += (custids == "" ? "" : ",") + custprop[1];     custcodes += (custcodes == "" ? "" : ",") + custprop[2];     custmanagers += (custmanagers == "" ? "" : ",") + custprop[3];     custnames += (custnames == "" ? "" : ",") + custprop[4];    }   }      dialog.setReturnValue(custids, custnames, ['USER_ID', 'CUST_CODE', 'CUST_MANAGER_ID'], [userids, custcodes, custmanagers]);  },    selectGroups : function() {   var custids = "", custnames = "", custcodes = "", custmanagers = "";      var custs = dom.getElements("custs");   for (var i=0; i<custs.length; i++) {    var cust = custs[i];    if (cust.checked) {     var custprop = cust.value.split(";");          custids += (custids == "" ? "" : ",") + custprop[0];     custcodes += (custcodes == "" ? "" : ",") + custprop[1];     custmanagers += (custmanagers == "" ? "" : ",") + custprop[2];     custnames += (custnames == "" ? "" : ",") + custprop[3];    }   }      dialog.setReturnValue(custids, custnames, ['CUST_CODE', 'CUST_MANAGER_ID'], [custcodes, custmanagers]);  },    queryPersonCusts : function(obj) {   var serial_number = dom.getElement("cond_SERIAL_NUMBER");   var pspt_id = dom.getElement("cond_PSPT_ID");   var cust_name = dom.getElement("cond_CUST_NAME");   if (serial_number.value == "" && pspt_id.value == "" && cust_name.value == "") {    alert("请输入一个或多个查询条件！\n注：客户名称不支持模糊查询！");    serial_number.focus();    return false;   }   return validate.queryAll(obj);  },    selectPersonCusts : function() {   var userids = "", custids = "", custnames = "", custcodes = "";      var custs = dom.getElements("custs");   for (var i=0; i<custs.length; i++) {    var cust = custs[i];    if (cust.checked) {     var custprop = cust.value.split(";");         userids += (userids == "" ? "" : ",") + custprop[0];     custids += (custids == "" ? "" : ",") + custprop[1];     custcodes += (custcodes == "" ? "" : ",") + custprop[2];     custnames += (custnames == "" ? "" : ",") + custprop[3];    }   }      dialog.setReturnValue(custids, custnames, ['USER_ID', 'CUST_CODE'], [userids, custcodes]);  },    selectVipCusts : function() {   var userids = "", custids = "", custnames = "", custcodes = "", vipids = "", custmanagers = "";      var custs = dom.getElements("custs");   for (var i=0; i<custs.length; i++) {    var cust = custs[i];    if (cust.checked) {     var custprop = cust.value.split(";");          userids += (userids == "" ? "" : ",") + custprop[0];     custids += (custids == "" ? "" : ",") + custprop[1];     custcodes += (custcodes == "" ? "" : ",") + custprop[2];     vipids += (vipids == "" ? "" : ",") + custprop[3];     custmanagers += (custmanagers == "" ? "" : ",") + custprop[4];     custnames += (custnames == "" ? "" : ",") + custprop[5];    }   }      dialog.setReturnValue(custids, custnames, ['USER_ID', 'CUST_CODE', 'VIP_ID', 'CUST_MANAGER_ID'], [userids, custcodes, vipids, custmanagers]);  } }})();